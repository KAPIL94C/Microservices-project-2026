/*
 This file previously contained YAML that was accidentally committed as a .java file.
 The real configuration is in src/main/resources/application.yml.
 Keeping this placeholder as a commented note so the resources folder documents the change
 but no Java class is declared here (prevents compilation/package errors).
*/
